<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="its.me.baby.mapper.UserMapper">
	
	<resultMap id="user" type="its.me.baby.dto.User">
		<id property="id" column="ID"/>
		<result property="email" column="EMAIL"/>
		<result property="cryptoPassword" column="CRYPTO_PASSWORD"/>
		<result property="name" column="NAME"/>
		<result property="profile" column="PROFILE"/>
	</resultMap>
	
	<select id="newId" resultType="int">
		SELECT SEQ_USER.NEXTVAL
	</select>

	<select id="getUserById" parameterType="its.me.baby.dto.UserGetter" resultMap="user">
		SELECT
			ID,
			EMAIL,
			CRYPTO_PASSWORD,
			NAME,
			PROFILE
		FROM
			USER
		WHERE
			ID = #{id} AND CRYPTO_PASSWORD = #{cryptoPassword}
	</select>

	<select id="getUserByEmail" parameterType="its.me.baby.dto.UserGetter" resultMap="user">
		SELECT
			ID,
			EMAIL,
			CRYPTO_PASSWORD,
			NAME,
			PROFILE
		FROM
			USER
		WHERE
			EMAIL = #{email} AND CRYPTO_PASSWORD = #{cryptoPassword}
	</select>

	<select id="countUserByEmail" parameterType="String" resultType="int">
		SELECT
			COUNT(*)
		FROM
			USER
		WHERE
			EMAIL = #{email}
	</select>

	<insert id="saveUser" parameterType="its.me.baby.dto.User">
		INSERT INTO USER
		(
			ID,
			EMAIL,
			CRYPTO_PASSWORD,
			NAME,
			PROFILE
		)
		VALUES
		(
			#{id},
			#{email},
			#{cryptoPassword},
			#{name},
			#{profile}
		)
	</insert>
</mapper>